export interface Product {
    id: String;
    title: String;
    description: String;
    picture_url: String;
    quantity: Number;
    currency_id: String;
    unit_price: Number;
    category_id: String
}